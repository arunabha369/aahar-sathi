# Where the Express API lives. Used by the /api rewrite and by Server Components.
# Needed at build time too, because rewrites are generated during the build.
API_URL=http://localhost:5001

# Canonical site URL, used for metadataBase, robots.txt and sitemap.xml
NEXT_PUBLIC_SITE_URL=https://aaharsathi.in
